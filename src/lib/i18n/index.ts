/**
 * Re-export from the single content source.
 * Prefer importing from `@/lib/content` directly.
 */
export {
  brand,
  defaultLocale,
  dictionaries,
  en,
  getDictionary,
  isLocale,
  localeLabels,
  locales,
  ru,
  type Locale,
  type SiteContent,
} from "@/lib/content";
