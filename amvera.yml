meta:
  environment: node
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: |
    npm install
run:
  command: npx next build && npx next start -H 0.0.0.0 -p 3000
  containerPort: 3000
